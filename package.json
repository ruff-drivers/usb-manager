{
    "name": "usb-manager",
    "version": "0.2.2",
    "description": "Build usb dynamic device manager.",
    "author": "Nanchao Inc.",
    "main": "src/index.js",
    "engines": {
        "ruff": ">=1.4.0 <2.0.0"
    },
    "scripts": {
        "lint": "eslint *.js",
        "pretest": "npm run lint",
        "test": "ruff ./test/test.js"
    },
    "devDependencies": {
        "eslint": "^2.11.1",
        "ruff-standard": "^0.1.4",
        "rvm": "^0.3.2"
    },
    "ruff": {
        "devDependencies": {
            "t": "^0.1.6"
        }
    }
}
